# Blanket Cat Pet Files

This folder is the installable Codex pet package.

To install manually, copy this entire `blanket-cat` folder to:

```text
~/.codex/pets/blanket-cat
```

Then restart Codex and select `Blanket Cat`.
